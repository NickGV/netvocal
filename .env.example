# ── Backend (FastAPI) ──────────────────────────────────────────────────
# VA_OPENAI_API_KEY=sk-...
# VA_STT_PROVIDER=mock|openai
# VA_WHISPER_MODEL=whisper-1
# VA_LLM_PROVIDER=mock|openai
# VA_LLM_MODEL=gpt-4o-mini
# VA_CORS_ALLOW_ORIGINS=["http://localhost:3000"]

# ── Frontend (Next.js) ────────────────────────────────────────────────
NEXT_PUBLIC_API_URL=http://localhost:8000
